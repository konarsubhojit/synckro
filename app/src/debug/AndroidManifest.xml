<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only additions to the app manifest.
  Registers a FileProvider so the log-export button in AccountsScreen can
  produce a content:// URI for the debug log file without requiring any
  storage permission. This provider is absent from release builds.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/debug_log_file_paths" />
        </provider>
    </application>
</manifest>
